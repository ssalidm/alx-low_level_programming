#include "main.h"

/**
* _abs - computes the absolute value of an integer.
*
* @num: value to calculate
*
* Return: Absolute value of n.
*/

int _abs(int num)
{
if (num < 0)
{
return (num * -1);
}
else
{
return (num);
}
}
